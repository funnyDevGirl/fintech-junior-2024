spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/javalessons
    username: alishka
    password: a111dev

  liquibase:
    drop-first: true
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yml
    default-schema: public

  jpa:
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect